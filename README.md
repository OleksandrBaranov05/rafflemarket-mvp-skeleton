# RaffleMarket - Маркетплейс розіграшів реальних товарів

**RaffleMarket** — це платформа, де продавці виставляють реальні товари (автомобілі, будинки, техніка, коштовності), а користувачі купують квитки. Коли всі квитки продані, один переможець обирається випадковим чином і отримує приз. Платформа бере комісію з кожного розіграшу.

## 🚀 Технології

- **Next.js 15** (App Router)
- **TypeScript** (повна типізація)
- **CSS Modules** (*.module.css)
- **TanStack React Query** (серверний стан, кешування, оновлення)
- **Formik + Yup** (форми та валідація)
- **react-hot-toast** (сповіщення)
- **Zustand** (клієнтський стан, тільки за потреби)

## 📦 Як запустити

### Вимоги
- Node.js 18+ 
- npm або yarn

### Встановлення

1. Клонуйте репозиторій:
```bash
git clone <repository-url>
cd rafflemarket-mvp-skeleton
```

2. Встановіть залежності:
```bash
npm install
```

3. Створіть файл `.env.local` (опціонально):
```env
SESSION_SECRET=your-secret-key-here
NEXT_PUBLIC_BASE_URL=http://localhost:3000
```

4. Запустіть dev-сервер:
```bash
npm run dev
```

5. Відкрийте браузер:
```
http://localhost:3000
```

## 🔐 Демо облікові записи

- **Продавець**: `demo@demo.com` / `password`
- **Адміністратор**: `admin@admin.com` / `admin`

## 📁 Структура проєкту

```
rafflemarket-mvp-skeleton/
├── app/                          # Next.js App Router
│   ├── (auth)/                   # Публічні auth сторінки
│   │   ├── login/
│   │   └── register/
│   ├── (private)/                # Приватні сторінки (захищені)
│   │   ├── dashboard/            # Кабінет користувача
│   │   ├── profile/              # Профіль користувача
│   │   ├── admin/                # Адмін-панель
│   │   └── me/                   # Мої квитки
│   ├── (public)/                 # Публічні сторінки
│   │   ├── raffles/              # Каталог та деталі лотів
│   │   ├── lots/[id]/result/     # Результати розіграшів
│   │   ├── privacy/              # Політика конфіденційності
│   │   └── terms/                # Умови використання
│   ├── api/                      # API маршрути
│   │   ├── auth/                 # Аутентифікація
│   │   ├── raffles/              # Розіграші
│   │   ├── tickets/              # Квитки
│   │   └── profile/              # Профіль
│   ├── error.tsx                 # Глобальна обробка помилок
│   ├── not-found.tsx             # 404 сторінка
│   └── layout.tsx                # Кореневий layout
├── components/                   # Перевикористовувані UI компоненти
│   ├── Header/                   # Головна навігація
│   ├── Footer/                   # Футер
│   ├── Loader/                   # Індикатори завантаження
│   ├── StatusBlock/              # Блоки статусів (помилки/успіх)
│   ├── Pagination/               # Пагінація
│   └── CreateRaffleLink/         # Компонент для створення лотів
├── lib/                          # Утиліти та helpers
│   ├── api/                      # API функції (один файл = один запит)
│   ├── auth/                     # Аутентифікація (session, guards)
│   ├── store/                    # In-memory database (MVP)
│   ├── types/                    # TypeScript типи
│   ├── utils/                    # Утиліти (http, queryKeys, sleep)
│   └── metadata/                 # Генератори metadata
├── providers/                    # React провайдери
│   ├── Providers.tsx             # Головний провайдер
│   ├── ReactQueryProvider.tsx    # TanStack React Query
│   └── ToasterProvider.tsx       # react-hot-toast
└── store/                        # Zustand stores (за потреби)
```

## ✨ Ключові функції

### ✅ Реалізовано
- ✅ Публічний каталог лотів (`/raffles`) з пошуком, фільтрацією та сортуванням
- ✅ Детальна сторінка лоту з купівлею квитків (`/raffles/[id]`)
- ✅ Відображення моїх квитків на сторінці лоту
- ✅ Кабінет користувача (`/dashboard`) - створення та перегляд своїх лотів
- ✅ Статистика продавця (дохід, продажі, комісії)
- ✅ Історія покупок користувача (`/me/tickets`) - мої квитки
- ✅ Адмін-панель модерації (`/admin`) - затвердження/відхилення лотів
- ✅ Автоматичний випадковий вибір переможця при продажу всіх квитків
- ✅ Реєстрація та авторизація користувачів
- ✅ Профіль користувача з можливістю редагування
- ✅ Перемикач режиму продавця/покупця
- ✅ Результати розіграшів (`/lots/[id]/result`)
- ✅ Адаптивний дизайн (mobile-first)

### 🔄 В розробці
- ⏳ Редагування лотів
- ⏳ Видалення лотів
- ⏳ Профіль продавця (`/profile/[userId]`)
- ⏳ Модалки через parallel routes
- ⏳ Платежна система (MVP)

## 🎯 Архітектура

### Аутентифікація
- Сесійна аутентифікація через httpOnly cookies
- HMAC-підписані payload для безпеки
- Server-side guards (`requireSession()`, `requireAdmin()`)

### Стейт-менеджмент
- **Серверний стан**: TanStack React Query
  - Кешування запитів
  - Автоматичне оновлення
  - Інвалідація при мутаціях
- **Клієнтський стан**: Zustand (тільки за потреби)
  - UI стан (модалки, меню)
  - Користувацькі налаштування

### Форми
- **Formik** для управління станом форм
- **Yup** для валідації
- Обов'язкова валідація перед відправкою

### Стилізація
- **ТІЛЬКИ CSS Modules** (*.module.css)
- Mobile-first підхід
- Адаптивність для всіх пристроїв

## 📝 Env змінні

```env
# Обов'язкові
SESSION_SECRET=your-secret-key-change-in-production

# Опціональні
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Майбутні (для платежів)
# PAYMENT_PROVIDER_API_KEY=
# PAYMENT_WEBHOOK_SECRET=
```

## 🔒 Безпека

- HttpOnly cookies для сесій
- HMAC підпис для захисту cookie
- Server-side перевірки прав доступу
- Валідація даних на клієнті та сервері
- Захист приватних маршрутів

## 📄 Ліцензія

MIT

## 👥 Контрибуції

Вітаються pull requests та issues!
